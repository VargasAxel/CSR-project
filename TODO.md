# TODO

## Teal-primary palette update
- [ ] Update `:root` tokens in `style.css` to make teal the primary accent (add accent aliases).
- [ ] Replace major gold-based UI emphasis styles with teal equivalents (nav underline, section bars/titles, primary buttons, borders/lines, focus-visible).
- [ ] Keep gold as a secondary/sparingly used luxury accent.
- [ ] Search `style.css` for remaining `--gold` / gold hardcodes and review if they should stay.
- [ ] Final smoke test: open `index.html` in browser and verify key UI states (hover/focus/modal).


